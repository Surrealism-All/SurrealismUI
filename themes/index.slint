import {ThemeColor} from "./src/color/color.slint";
import {ThemeFont} from "./src/font/font.slint";
import {ThemePadding,ThemeBorder} from "./src/padding/padding.slint";
import {Size,SizeStruct} from "./src/common.slint";
import { Icons } from "./src/icon.slint";
export { Icons,Size}

export enum Themes{
  Primary,
  Success,
  Info,
  Warning,
  Error,
  Dark
}

export global ROOT_STYLES {
  out property <ThemeFont> sur-font:{
    font-family:"Verdana",
    font-weight:400,
    font-size:16px,
    font-italic:false,
    letter-spacing:1px,
    tag-size:12px
  };
  out property <brush> font-light : #ffffff;
  out property <brush> font-black : #000;
  in-out property <ThemeColor> sur-theme-colors : {
    primary:{name:"primary",weakest:#96C4ED,weaker:#4584E9,normal:#1A5BE9,deeper:#0F3CC9,deepest:#1d2f7a,font:#bbdbf6,opacity:#1A5BE988},
    success:{name:"success",weakest:#B0E5DC,weaker:#7FD5A2,normal:#66CD7A,deeper:#4aa949,deepest:#33956B,font:#e9fffb,opacity:#7de39187},
    info:{name:"info",weakest:#E6E5E6,weaker:#DEDDDE,normal:#cecece,deeper:#bcbcbc,deepest:#878787,font:#ffffff,opacity:#d7d7d788},
    warning:{name:"warning",weakest:#e48d73,weaker:#f07651,normal:#f06b42,deeper:#e95a2e,deepest:#e63819,font:#fdd1c3,opacity:#dd5f32c9},
    error:{name:"error",weakest:#e9a9a7,weaker:#DC8472,normal:#e34e47,deeper:#D03D46,deepest:#9e2929,font:#fbe3d4,opacity:#e34e4788},
    dark:{name:"dark",weakest:#2f323d,weaker:#171922,normal:#1a1c26,deeper:#0f121c,deepest:#101114,font:#73788c,opacity:#262a3987}
  };
  out property <ThemePadding> sur-padding : {
    small: {top-bottom:sur-font.font-size / 3.8,left-right:sur-font.font-size / 3.2,same:sur-font.font-size / 3.6},
    normal:{top-bottom:sur-font.font-size / 1.8,left-right:sur-font.font-size / 1.6,same:sur-font.font-size / 1.7},
    large:{top-bottom:sur-font.font-size / 1.2,left-right:sur-font.font-size / 1,same:sur-font.font-size / 1.1}
  };
  out property <ThemeBorder> sur-border:{
    small:{border-radius:4px,border-width:1px},
    normal:{border-radius:6px,border-width:1.2px},
    large:{border-radius:9px,border-width:1.8px},
    circle:1000in,
  };
  out property <percent> sur-opacity : 86%;
  out property <duration> sur-an-duration :450ms;
  out property <easing> sur-an-easing : ease-in-out;
  in-out property <SizeStruct> sur-size:{
    small:12px,
    normal:16px,
    large:20px,
  };
  out property <length> scroll-bar-width : 14px;
}


