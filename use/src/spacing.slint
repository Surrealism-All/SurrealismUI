export struct ThemeSpace {
  none:length,
  len20:length,
  len40:length,
  len60:length,
  len80:length,
  len120:length,
  len160:length,
  len200:length,
  len240:length,
  len280:length,
  len320:length,
  len360:length,
  len400:length,
  len440:length,
  len480:length,
  len520:length,
  len560:length,
}
