import { Themes } from "./theme.slint";
import { Position } from "./position.slint";

export struct SPopoverProps {
  theme : Themes,
  position : Position,
  is-show : bool,
  owner-height:length,
  owner-width:length
}