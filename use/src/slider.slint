import { Themes } from "./theme.slint";
export struct SSliderProps {
    orientation: Orientation,
    theme :Themes,
    progress : float,
    font-weight : int,
    font-size: length,
    font-color : brush,
    font-italic : bool,
    font-family : string,
    stroke-width: length,
    stroke-color: brush,
    border-radius: length,
}