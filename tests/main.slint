import { TabWidget , ScrollView} from "std-widgets.slint";
import {SText,SLink} from "../index.slint";
import { TestColor } from "./src/color.slint";
import {TestIcon} from "./src/Icon.slint";
import {TestCard} from "./src/card.slint";
import {TestButton} from "./src/button.slint";
import {TestInput} from "./src/input.slint";
import { TestResult } from "./src/result.slint";
import {TestTable} from "./src/table.slint";
export component MyLib inherits Window {
    height: 720px;
    width: 1080px;
    background: #4b4b4b;
    title:"Surrealism";
    TabWidget {
      Tab {
        title: "Theme Colors";
        SText {
          x: 16px;
          y: 16px;
          text: "Surrealism UI V0.1.0 - Theme Colors";
          font-weight: 700;
          font-size: 32px;
        }
        SLink {
          x: 16px;
          y: 60px;
          text: "syf20020816@outlook.com";
          theme: Error;
          funny:true;
        }
        ScrollView { 
          y: 100px;
          height: 560px;
          width: 1024px;
          viewport-height: 1400px;
          TestColor {
            
          }
        }
      }
      Tab {
        title: "Icons";
        SText {
          x: 16px;
          y: 16px;
          text: "Surrealism UI V0.1.0 - Theme Icons";
          font-weight: 700;
          font-size: 32px;
        }
        SText {
          x: 16px;
          y: 60px;
          text: " there are 2658 different icons in SIcon from : https://github.com/bytedance/iconpark";
          font-weight: 700;
        }
        Rectangle {
          y:150px;
          height: icons.height;
          width: icons.width;
          icons:=TestIcon {
            
          }
        }
      }
      Tab {
        title: "Cards";
        SText {
          x: 16px;
          y: 16px;
          text: "Surrealism UI V0.1.0 - Theme Cards";
          font-weight: 700;
          font-size: 32px;
        }
        SText {
          x: 16px;
          y: 60px;
          text: "A very simple universal card without any layout or restrictions , you can add anything you want to the card";
          font-weight: 700;
        }
        Rectangle {
          y:100px;
          height: view.height;
          width: view.width;
          view:=TestCard{
          }
        }
      }
      Tab {
        title: "Buttons";
        SText {
          x: 16px;
          y: 16px;
          text: "Surrealism UI V0.1.0 - Theme Buttons";
          font-weight: 700;
          font-size: 32px;
        }
        SText {
          x: 16px;
          y: 60px;
          text: "SButton is a button component that you can freely perform regular attribute operations on";
          font-weight: 700;
        }
        Rectangle {
          y:100px;
          height: view2.height;
          width: view2.width;
          view2:=TestButton{
          }
        }
      }
      Tab {
        title: "Inputs";
        SText {
          x: 16px;
          y: 16px;
          text: "Surrealism UI V0.1.0 - Theme Inputs";
          font-weight: 700;
          font-size: 32px;
        }
        SText {
          x: 16px;
          y: 60px;
          text: "This is a basic input box, often used in forms, divided into two types : text and password";
          font-weight: 700;
        }
        Rectangle {
          y:100px;
          height: view3.height;
          width: view3.width;
          view3:=TestInput{}
        }
      }
      Tab {
        title: "Results";
        SText {
          x: 16px;
          y: 16px;
          text: "Surrealism UI V0.1.0 - Theme Results";
          font-weight: 700;
          font-size: 32px;
        }
        SText {
          x: 16px;
          y: 60px;
          text: "SResult helps you easily build a quick prompt , you can build it in popup window";
          font-weight: 700;
        }
        Rectangle {
          y:100px;
          height: view4.height;
          width: view4.width;
          view4:=TestResult{}
        }
      }
      Tab {
        title: "Tables";
        SText {
          x: 16px;
          y: 16px;
          text: "Surrealism UI V0.1.0 - Theme Tables";
          font-weight: 700;
          font-size: 32px;
        }
        SText {
          x: 16px;
          y: 60px;
          text: "This is the outter of the Table, and the column data of the table is separated from the outter . The outter only serves as a standard layout , this is a zero cost construction";
          font-weight: 700;
          wrap:word-wrap;
          width:1024px;
        }
        Rectangle {
          y:100px;
          height: view5.height;
          width: view5.width;
          view5:=TestTable{}
        }
      }
  }
}