import { SSwitchGroup } from "../../index.slint";

component TestSwitchGroup inherits Window {
  height: 400px;
  width: 400px;
  SSwitchGroup {
   theme: Primary;
    clicked(i,name) => {
      debug(i);
      debug(name);
    }
  }
  SSwitchGroup {
    y: 120px;
    theme:Dark;
    switchs:["1","2","3","4"];
     clicked(i,name) => {
       debug(i);
       debug(name);
     }
   }
}